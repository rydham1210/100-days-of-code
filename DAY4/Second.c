# include<stdio.h>
int main()
{
    int n,sum;
    printf("enter the vakue of n");
    scanf("%d", n);
    sum =n*(n+1)/2;
    printf("sum of first natural number is %d",n,sum);
    return 0;
}